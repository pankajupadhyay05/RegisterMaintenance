<Page x:Class="RegisterMaintenance.Ledger"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="900"
	Title="Ledger">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <GroupBox Style="{StaticResource centeredHeaderGroupBox0Style}" Grid.Column="1" Grid.ColumnSpan="1" Header="Ledger" Height="Auto" HorizontalAlignment="Stretch" Margin="0,100,0,0" Name="groupBox1" VerticalAlignment="Top" Width="265">
            <Grid>
                <Button Content="Create" Height="23" HorizontalAlignment="Stretch" Margin="40,20,40,0" Name="createButton" VerticalAlignment="Top" Width="Auto" Click="createButton_Click" />
                <Button Content="Display/Edit" Height="23" HorizontalAlignment="Stretch" Name="displayRecordButton" VerticalAlignment="Top" Width="Auto" Margin="40,60,40,0" Click="displayRecordButton_Click" />
                <Button Content="Exit" Height="23" HorizontalAlignment="Stretch" Margin="40,100,40,20" Name="exitButton" VerticalAlignment="Top" Width="Auto" Click="exitButton_Click" />
            </Grid>
        </GroupBox>

    </Grid>
</Page>
